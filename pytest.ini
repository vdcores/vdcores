[pytest]
testpaths = tests
pythonpath =
    python
    tests
markers =
    build: build-system tests
    no_gpu: tests that must not require CUDA devices
    gpu: tests that require a CUDA device
    perf: GPU performance smoke tests
